{
  "mcpServers": {
    "hive-mcp": {
      "type": "stdio",
      "command": "cmd",
      "args": ["/c", "npx", "-y", "github:ocuco/hive-mcp"],
      "env": {
        "JIRA_BASE_URL": "${JIRA_BASE_URL}",
        "JIRA_EMAIL": "${JIRA_EMAIL}",
        "JIRA_API_TOKEN": "${JIRA_API_TOKEN}",
        "SOURCEGRAPH_URL": "${SOURCEGRAPH_URL}",
        "SOURCEGRAPH_TOKEN": "${SOURCEGRAPH_TOKEN}"
      }
    }
  }
}
